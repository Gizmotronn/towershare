# Copy to .env and fill in values
# Generate a key: openssl rand -hex 32
PB_ENCRYPTION_KEY=

# Used by the Swift client in dev
PB_URL=http://localhost:8090

# Set to "production" to disable auto-migrations
APP_ENV=development
